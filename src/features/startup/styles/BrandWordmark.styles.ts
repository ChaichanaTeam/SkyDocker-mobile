import { StyleSheet } from 'react-native';

import { colors, typography } from '@/theme';

export const styles = StyleSheet.create({
  text: {
    color: colors.startupLogo,
    fontFamily: typography.interRegular,
    fontStyle: 'normal',
    fontWeight: '400',
    letterSpacing: 0,
    textAlign: 'center',
  },
});

